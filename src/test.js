import { MapContainer, TileLayer, useMap } from 'react-leaflet';


export function Map() {

    return (
      <MapContainer>
  
        <TileLayer />  
  
        const map = useMap();
  
      </MapContainer>
    )
  
  };